<!--pages/main-video/main-video.wxml-->
<view class="video-list">
    <block wx:for="{{ videoList }}" wx:key="id">
        <!-- video-item不是组件根元素，flex布局无法直接作用到组件里的根元素，必须要手动设置 -->
        <video-item class="item" itemData="{{item}}"></video-item>
    </block>
</view>